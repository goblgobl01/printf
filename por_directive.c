#include "main.h"

/**
 * por_directive - Prints a '%' character
 *
 * This function prints a '%' character to the standard output.
 *
 * @c: The character to be printed
 *
 * Return: Always returns 1.
 */
int por_directive(char c)
{
	_putchar(c);
	return (1);
}
